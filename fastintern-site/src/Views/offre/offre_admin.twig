<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestion des Offres</title>
    <link rel="stylesheet" href="http://fastintern-static.local/static/css/admin.css">
</head>
<body>
    <header>
        <div class="logo">
            <img src="http://fastintern-static.local/static/assets/logo.png" alt="Logo FastIntern">
            <span>FastIntern - Administration</span>
        </div>
        <nav>
            <a href="/?uri=index">Accueil</a>
            <a href="/?uri=admin/offres" class="active">Gestion des Offres</a>
            <a href="/?uri=logout" class="btn-logout">Déconnexion</a>
        </nav>
    </header>

    <main class="admin-container">
        <div class="actions-header">
            <h1>Gestion des Offres</h1>
            <button class="btn-create" onclick="toggleForm()">Créer une nouvelle offre</button>
        </div>

        <!-- Formulaire de création/modification -->
        <div id="offreForm" class="form-container" style="display: none;">
            <h2 id="formTitle">Créer une nouvelle offre</h2>
            <form action="/?uri=admin/offres/save" method="POST">
                <input type="hidden" name="id_offre" id="id_offre">

                <div class="form-group">
                    <label for="titre">Titre *</label>
                    <input type="text" id="titre" name="titre" required>
                </div>

                <div class="form-group">
                    <label for="description">Description *</label>
                    <textarea id="description" name="description" required rows="5"></textarea>
                </div>

                <div class="form-group">
                    <label for="remuneration">Rémunération (€/mois)</label>
                    <input type="number" id="remuneration" name="remuneration" min="0">
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label for="date_debut">Date de début *</label>
                        <input type="date" id="date_debut" name="date_debut" required>
                    </div>
                    <div class="form-group">
                        <label for="date_fin">Date de fin *</label>
                        <input type="date" id="date_fin" name="date_fin" required>
                    </div>
                </div>

                <div class="form-group">
                    <label for="nom_entreprise">Nom de l'entreprise *</label>
                    <input type="text" id="nom_entreprise" name="nom_entreprise" required>
                </div>

                <div class="form-group">
                    <label for="email_contact">Email de contact *</label>
                    <input type="email" id="email_contact" name="email_contact" required>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn-submit">Enregistrer</button>
                    <button type="button" class="btn-cancel" onclick="toggleForm()">Annuler</button>
                </div>
            </form>
        </div>

        <div class="search-container">
            <form action="/?uri=admin/offres" method="GET" class="search-form">
                <input type="hidden" name="uri" value="admin/offres">
                <input type="text" name="search" placeholder="Rechercher par nom d'entreprise" value="{{ search|default('') }}">
                <button type="submit" class="btn-search">Rechercher</button>
            </form>
        </div>

        <!-- Tableau des offres -->
        <div class="table-container">
            <table class="offres-table">
                <thead>
                    <tr>
                        <th>Titre</th>
                        <th>Entreprise</th>
                        <th>Date début</th>
                        <th>Date fin</th>
                        <th>Rémunération</th>
                        <th>Statut</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for offre in offres %}
                    <tr>
                        <td>{{ offre.titre }}</td>
                        <td>{{ offre.nom_entreprise }}</td>
                        <td>{{ offre.date_debut|date('d/m/Y') }}</td>
                        <td>{{ offre.date_fin|date('d/m/Y') }}</td>
                        <td>{{ offre.remuneration ? offre.remuneration ~ ' €' : 'Non précisée' }}</td>
                        <td>{{ offre.statut }}</td>
                        <td class="actions">
                            <button onclick="editOffre({{ offre.id_offre }}, '{{ offre.titre }}', '{{ offre.description }}', {{ offre.remuneration }}, '{{ offre.date_debut }}', '{{ offre.date_fin }}', '{{ offre.nom_entreprise }}')" class="btn-edit">Modifier</button>
                            <button onclick="deleteOffre({{ offre.id_offre }})" class="btn-delete">Supprimer</button>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </main>

    <script src="http://fastintern-static.local/static/js/Admin/offre.js"></script>

</body>
</html>