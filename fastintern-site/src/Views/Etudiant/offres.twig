<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Offres Étudiant</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="http://fastintern-static.local/static/css/Etudiant/offres.css">
    <link rel="stylesheet" href="http://fastintern-static.local/static/css/layout/header.css">
    <link rel="stylesheet" href="http://fastintern-static.local/static/css/layout/footer.css">
</head>
<body>
    <header>
        <div class="logo">
            <img src="http://fastintern-static.local/static/assets/logo.png" alt="Logo FastIntern">
            <span class="titre-logo">FastIntern</span>
        </div>
        <div class="nav-container">
            {% if session.user_id is defined %}
                {% if session.role_id == 3 %}
                    <nav>
                <a class="btn-accueil" href="?uri=etudiant">Accueil</a>
                <a class="btn-accueil" href="?uri=offres">Voir Offres</a>
                <a class="btn-accueil" href="#">Voir Entreprises</a>
            </nav>
            
            <div class="profile-container">
                <a class="btn-profile" href="?uri=etudiant_profil">
                    <i class="fas fa-user-circle"></i>
                    Mon Profil
                </a>
            </div>
                {% endif %}
            {% endif %}
        </div>

        <div class="burger-menu" onclick="toggleMenu()">
            <div class="bar"></div>
            <div class="bar"></div>
            <div class="bar"></div>
        </div>

    </header>

    <!-- slogan et rectangle bleu -->
    <section class="mid-section">
        <div class="highlight">
            <img src="http://fastintern-static.local/static/assets/cloud.png" alt="Mid Section Image">
            <span>Votre stage, en un éclair !</span>
        </div>
    </section>

    <section class="offers-section">
    <div class="offers-container">
        {% for offre in offres %}
            <div class="offer-block">
                <h3>{{ offre.titre }}</h3>
                <p class="company"><strong>Entreprise :</strong> {{ offre.nom_entreprise }}</p>
                <p class="description">
                    {{ offre.description|length > 150 ? offre.description|slice(0, 150) ~ '...' : offre.description }}
                </p>
                <p class="remuneration"><strong>Rémunération :</strong> {{ offre.remuneration ? offre.remuneration ~ ' €' : 'Non précisée' }}</p>
                <p class="dates">
                    <strong>Début :</strong> {{ offre.date_debut|date('d/m/Y') }}<br>
                    <strong>Fin :</strong> {{ offre.date_fin|date('d/m/Y') }}
                </p>
                <p class="publication-date"><em>Publié le {{ offre.date_publication|date('d/m/Y') }}</em></p>
                <a href="/?uri=detail&id={{ offre.id_offre }}" class="details-link">Voir les détails</a>
            </div>
        {% else %}
            <p>Aucune offre disponible pour le moment.</p>
        {% endfor %}
    </div>

    <!-- Pagination -->
    <div class="pagination">
    {% if pagination.previous %}
        <a href="/?uri=offres&page={{ pagination.previous }}" class="prev">Précédent</a>
    {% endif %}

    {% for page in pagination.pages %}
        <a href="/?uri=offres&page={{ page }}" class="{{ page == pagination.current ? 'active' : '' }}">{{ page }}</a>
    {% endfor %}

    {% if pagination.next %}
        <a href="/?uri=offres&page={{ pagination.next }}" class="next">Suivant</a>
    {% endif %}
</div>
    </section>

   <footer>
        <div class="footer-content">
            <div class="footer-columns">
                {% if footerColumns is defined and footerColumns|length > 0 %}
                    {% for column in footerColumns %}
                        <div class="footer-column">
                            <h3>{{ column.title }}</h3>
                            <ul>
                                {% for link in column.links %}
                                    <li><a href="{{ link.url }}">{{ link.text }}</a></li>
                                {% endfor %}
                            </ul>
                        </div>
                    {% endfor %}
                {% else %}
                    <div class="footer-column">
                        <h3>L'emploi</h3>
                        <ul>
                            <li><a href="#">Offres d'emploi par ville</a></li>
                            <li><a href="#">Offres d'emploi par secteurs</a></li>
                            <li><a href="#">Offres d'emploi par entreprises</a></li>
                            <li><a href="#">Offres d'emploi par mots clés</a></li>
                        </ul>
                    </div>
                    <div class="footer-column">
                        <h3>L'entreprise</h3>
                        <ul>
                            <li><a href="#">Qui sommes-nous ?</a></li>
                            <li><a href="#">On recrute</a></li>
                            <li><a href="#">Accès client</a></li>
                        </ul>
                    </div>
                    <div class="footer-column">
                        <h3>Mentions légales</h3>
                        <ul>
                            <li><a href="?uri=infos_legales">Informations légales</a></li>
                            <li><a href="?uri=cgu">CGU</a></li>
                            <li><a href="?uri=politique_confidentialite">Politique de confidentialité</a></li>
                            <li><a href="#">Aide et contact</a></li>
                        </ul>
                    </div>
                {% endif %}
            </div>

            <div class="footer-social">
                    <a href="#"><img src="http://fastintern-static.local/static/assets/instagram.png" alt="Instagram"></a>
                    <a href="#"><img src="http://fastintern-static.local/static/assets/x.png" alt="X"></a>
                    <a href="#"><img src="http://fastintern-static.local/static/assets/linkedin.png" alt="LinkedIn"></a>
                    <a href="#"><img src="http://fastintern-static.local/static/assets/facebook.png" alt="Facebook"></a>
                    <a href="#"><img src="http://fastintern-static.local/static/assets/youtube.png" alt="YouTube"></a>
            </div>
            
            <div class="footer-copyright">
                <p>&copy; {{ "now"|date("Y") }} FastIntern - Tous droits réservés</p>
            </div>
        </div>
    </footer>

    <script src="http://fastintern-static.local/static/JS/Etudiant/offres.js"></script>
    <script src="http://fastintern-static.local/static/JS/layout/header.js"></script>
    <script src="http://fastintern-static.local/static/JS/layout/footer.js"></script>
</body>
</html>